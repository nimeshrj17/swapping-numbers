#include<stdio.h>
int main()
{
	int a,b,c;
	printf("enter any two numbers\n");
	scanf("%d%d",&a,&b);
	c=a>b?a:b;
	while(1)
	{if (c%a==0 && c%b==0)
	{printf("%d is the lcm",c);
	break; }
	c++;
	}
	return 0;
}
